package com.example.tdd.servicesTest;

import com.example.tdd.models.User;
import com.example.tdd.services.UserServiceImpl;
import org.junit.jupiter.api.Test;
import org.mockito.Mock;

import static org.assertj.core.api.BDDAssertions.then;

public class userServiceTest {
   /*@Mock //create and inject mocked instances
   UserServiceImpl userService;

   @Test
    void testAddUser(){
      //given
       User user=new User();
       user.setPhoneNumber("+212632236881");
       user.setEmail("ayoub@gmail.com");
       user.setUsername("ayoub mansouri");
       //when
       boolean isValid=userService.addUser(user);
       then(isValid).isTrue();
   }

    */

}
